[project]
name = "ap"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "datasets>=4.7.0",
    "numpy>=2.4.3",
    "pandas>=3.0.1",
]

[tool.setuptools.packages.find]
where = ["src"]
